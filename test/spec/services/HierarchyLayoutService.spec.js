'use strict';

describe('Service: HierarchyLayoutService', function () {

   // load the service's module
   beforeEach(module('emuWebAppApp'));

  // // instantiate service
   var HierarchyLayoutService;
   beforeEach(inject(function (_HierarchyLayoutService_) {
     HierarchyLayoutService = _HierarchyLayoutService_;
   }));

  /**
   *

  it('should findParents', function () {
    spyOn(LevelService, 'getLevelDetails').and.returnValue({ level: { items: []} });
    scope.modal.open('template','dataIn','dataExport');
    expect(scope.modal.dataIn).toEqual('dataIn');
    expect(scope.modal.dataExport).toEqual('dataExport');
    expect(scope.modal.templateUrl).toEqual('template');
    expect(scope.vs.setState).toHaveBeenCalled();    
  });
   */  

});
